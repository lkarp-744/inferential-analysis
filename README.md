# Inferential Analysis

This project is a pure Python re-implementation of the `inferential analysis` module found in https://github.com/StatNLP/empirical_methods without any dependency to `R` and `lme4`.

